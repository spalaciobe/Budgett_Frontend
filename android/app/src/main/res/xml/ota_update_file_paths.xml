<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- ota_update writes downloaded APKs to the app's external files dir;
         this entry exposes that directory through the FileProvider so the
         OS package installer can read the APK. -->
    <external-path name="ota_update" path="."/>
</paths>
