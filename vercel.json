{
  "buildCommand": "git clone --depth 1 -b stable https://github.com/flutter/flutter.git $HOME/flutter && export PATH=\"$HOME/flutter/bin:$PATH\" && flutter --version && flutter config --enable-web && flutter pub get && flutter build web --release",
  "outputDirectory": "build/web",
  "installCommand": "echo skip-install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
